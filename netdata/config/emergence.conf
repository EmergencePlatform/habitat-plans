{{#if bind.phpfpm.first ~}}
    EMERGENCE_RUNTIME='
        {{~ bind.phpfpm.first.pkg.origin }}/
        {{~ bind.phpfpm.first.pkg.name }}/
        {{~ bind.phpfpm.first.pkg.version }}/
        {{~ bind.phpfpm.first.pkg.release }}'
{{/if}}
